[pytest]
markers =
    slow: real end-to-end pipeline runs against repii/ sample images (tens of seconds each; real OCR/Docling, no mocking). Excluded from the default run -- use `pytest -m slow` (or point directly at tests/regression/) to include them.
addopts = -m "not slow"
