# Exact-pinned direct dependencies (see requirements.lock.txt for the full
# transitive-closure lockfile, generated via `uv pip compile requirements.txt
# -o requirements.lock.txt`). Two machines installing this file must resolve
# byte-identical direct dependency versions — no `>=` ranges. Bump versions
# deliberately (single PR, regenerate the lockfile, re-run
# tests/test_determinism.py + the repii/ regression fixtures) rather than
# letting them drift silently.
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
pillow==12.3.0
pytesseract==0.3.13
pymupdf==1.28.0
pdf2image==1.17.0
langdetect==1.0.9
phonenumbers==9.0.34
python-dotenv==1.2.2
easyocr==1.7.2
# rapidocr+onnxruntime replace paddleocr+paddlepaddle: same PP-OCR model
# lineage (converted to ONNX) but bundled in the wheel (no first-run network
# model download) and no paddlepaddle native/AVX-CPU-instruction dependency
# that silently fails to install on some machines. See
# app/services/ocr/engines.py.
rapidocr==3.9.2
onnxruntime==1.29.0
numpy==2.3.5
opencv-python-headless==5.0.0.93
scikit-image==0.26.0
docling==2.120.3
# Deterministic table/cell geometry via OpenCV border detection (see
# app/services/structure/table_geometry.py) — complements Docling, which is
# kept for general paragraph/header/footer block detection.
img2table==2.0.0
# Fast, dependency-light fuzzy matching with built-in prefix/subset
# detection (token_set_ratio) — replaces the hand-rolled difflib matcher in
# app/services/pii/name_matcher.py.
rapidfuzz==3.14.5
nicegui==3.16.0
pytest==9.1.1
pytest-asyncio==1.4.0
